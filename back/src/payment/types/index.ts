export enum PAYMENT_STATUS {
    PENDING = 'pending',
    PAID = 'paid',
    FAILED = 'failed',
    REFUNDED = 'refunded',
}

export enum LESSON_DURATION {
    MIN = 60,
    AVERAGE = 90,
    MAX = 120,
  }