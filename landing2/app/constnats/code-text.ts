export const codetext = `const сейчас = {
  понятно: false,
  уровень: "начинающий",
  вопросов: "много"
};

const менторы = {
  опыт: "10+ лет",
  подход: "индивидуальный",
  ответы: ["вовремя", "по делу", "с поддержкой"]
};

// 💡 Функция трансформации: от "ничего не понятно" к "всё получается!"
function начатьОбучение(ученик, наставники) {
  const результат = {
    уверенность: "растёт",
    знания: "закрепляются",
    успех: true,
    эмоции: "Сейчас — смутно. Скоро — ясно и круто!"
  };

  if (наставники.подход === "индивидуальный" && ученик.вопросов > 0) {
    return результат;
  }

  return { успех: false, сообщение: "Попробуй с нами — будет иначе!" };
}

// 🚀 Запускаем твой путь
 
}`